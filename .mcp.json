{
  "mcpServers": {
    "gemini": {
      "command": "node",
      "args": ["tools/gemini-mcp/index.js"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY:-}",
        "GEMINI_MODEL": "${GEMINI_MODEL:-gemini-2.5-pro}"
      }
    }
  }
}
