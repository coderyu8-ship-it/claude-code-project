# このリポジトリでの作業ルール

## Gemini の活用（常時ルール）

このプロジェクトには Gemini を呼び出す MCP サーバー `gemini` が設定されています。
利用できるツールは 2 つです。

| ツール | 用途 |
| --- | --- |
| `ask_gemini` | 自由な質問。セカンドオピニオン、別視点での検討、要約、案出し |
| `gemini_research` | Google 検索グラウンディング付きの調査。**出典 URL 付きで返る** |

### いつ使うか

以下の場合は、ユーザーから毎回指示されなくても、作業を始める前に自分から Gemini を呼ぶこと。

1. **教育・学習コンテンツを作るとき（最重要）**
   BNI 定例会の学習コーナー／エデュケーション、研修資料、勉強会スライド、解説記事など。
   まず `gemini_research` でテーマを調べ、得られた知見・具体例・データを構成に反映してから作る。
   自分の知識だけで書き始めない。

2. **事実・数字・固有名詞・最新情報を含む出力を作るとき**
   統計、制度、企業や書籍の情報、日付を伴う事柄。`gemini_research` で裏を取る。

3. **企画・構成・アイデア出しで、視点の広さが成果物の質を左右するとき**
   `ask_gemini` に同じお題を投げ、自分の案に無かった切り口を取り込む。

逆に、以下では呼ばない（時間の無駄になる）。
- 単純なファイル操作、コード修正、git 操作
- このリポジトリ内を読めば答えが出ること
- ユーザーが「Gemini は使わなくていい」と言ったとき

### 使い方の作法

- **プロンプトは具体的に。** 「BNI について」ではなく「BNI の定例会における 60 秒プレゼンで、
  紹介依頼を具体化することがなぜ成果につながるのか。実例と、よくある失敗パターンを含めて」のように、
  何を知りたいのかまで書く。
- **鵜呑みにしない。** Gemini の出力は素材であって成果物ではない。
  `gemini_research` が返した出典 URL に目を通し、怪しい断定は落とすか表現を弱める。
  出典が 0 件だった場合は、その情報は裏が取れていないものとして扱う。
- **統合して出す。** 「Gemini によると〜」と丸投げせず、自分の構成に溶かし込んで最終成果物にする。
  ただしユーザーが判断に使う重要な事実には、出典を添える。
- **失敗しても止まらない。** API キー未設定やエラーで Gemini が使えないときは、
  その旨を一言伝えたうえで、自分の知識だけで作業を続行する。ユーザーを待たせない。

## セットアップ

Gemini ツールには API キーが必要です。

1. https://aistudio.google.com/apikey でキーを取得
2. `cp .env.example .env` して `GEMINI_API_KEY=` に貼り付ける
3. Claude Code を再起動（`.mcp.json` は起動時に読まれるため）

`.env` は `.gitignore` 済みです。**キーを含むファイルを絶対にコミットしないこと。**
